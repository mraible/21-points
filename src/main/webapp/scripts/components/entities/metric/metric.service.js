'use strict';

angular.module('21pointsApp')
    .factory('Metric', function ($resource) {
        return $resource('api/metrics/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
